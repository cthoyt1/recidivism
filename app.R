require(shiny)

ui <- pageWithSidebar(
  headerPanel("PCA Examination"),
  sidebarPanel(),
  mainPanel()
)

server <- function(input, output) {
  
}

library(shiny)

ui <- fluidPage(
  
)

server <- function(input, output, session) {
  
}

shinyApp(ui, server)